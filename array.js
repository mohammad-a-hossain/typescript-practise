var person = {
    name: 'abu',
    age: 33,
    hobbies: ['fishing', 'running', 'fighting']
};
console.log(person.age, person.hobbies[1]);
var activites;
activites = ['suhel', 2343];
console.log(activites[0]);
for (var _i = 0, _a = person.hobbies; _i < _a.length; _i++) {
    var hobbu = _a[_i];
    console.log(hobbu);
}
